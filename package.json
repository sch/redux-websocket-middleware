{
  "private": true,
  "name": "redux-websocket-middleware",
  "version": "1.0.0",
  "description": "Declarative websockets for Redux.",
  "main": "dist/redux-websocket-middleware.js",
  "jsnext:main": "dist/redux-websocket-middleware.mjs",
  "module": "src/index.js",
  "scripts": {
    "build": "rollup --config",
    "start": "rollup --config --watch",
    "pretest": "yarn run build",
    "test": "standard && mocha --compilers js:buble/register",
    "prepublish": "yarn run test"
  },
  "keywords": ["redux", "websocket", "middleware", "action"],
  "author": "Adrian Schaedle",
  "license": "MIT",
  "devDependencies": {
    "mocha": "^3.1.1",
    "mock-socket": "^5.0.1",
    "rollup": "^0.36.3",
    "rollup-plugin-buble": "^0.14.0",
    "rollup-watch": "^2.5.0",
    "standard": "^8.3.0"
  }
}
